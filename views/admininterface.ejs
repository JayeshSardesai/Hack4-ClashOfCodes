<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Service Provider Dashboard</title>
        <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
        }
        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: calc(100vh - 80px);
        }
        .button {
            background-color: #28a745;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 15px 30px;
            font-size: 18px;
            cursor: pointer;
            margin: 10px;
            transition: background-color 0.3s;
            width: 250px; /* Set a fixed width for the buttons */
        }
        .button:hover {
            background-color: #218838;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            color: #777;
        }
    </style>
    </head>
    <body>

        <header>
            <h1>serviceRace Dashboard</h1>
            <p>Manage your services and orders efficiently</p>
        </header>

        <div class="container">
            <button class="button"
                onclick="window.location.href='/admininterface/edit'">Check
                Orders</button>
            <button class="button"
                onclick="window.location.href='/admininterface/verified'">Verified Profile
                </button>
            <button class="button"
                onclick="window.location.href= '/admininterface/approve'">Verify Profile</button> 
        </div>

        <div class="footer">
            <p>&copy; 2023 serviceRace. All rights reserved.</p>
        </div>

    </body>
</html>